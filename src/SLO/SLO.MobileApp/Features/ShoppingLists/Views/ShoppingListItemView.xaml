<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="SLO.MobileApp.Features.ShoppingLists.Views.ShoppingListItemView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:labels="clr-namespace:SLO.MobileApp.Controls.Labels"
    xmlns:models="clr-namespace:SLO.MobileApp.Core.Models.Foundations.ShoppingListItems;assembly=SLO.MobileApp.Core"
    x:DataType="models:ShoppingListItem">

    <Border
        Padding="10"
        Stroke="White"
        StrokeShape="RoundRectangle 10">

        <Grid
            ColumnDefinitions="*,Auto"
            RowDefinitions="*,Auto"
            RowSpacing="10">

            <labels:TitleLabel
                Grid.Row="0"
                Grid.Column="0"
                HorizontalOptions="Start"
                Text="{Binding Name}" />

            <labels:BodyLabel
                Grid.Row="0"
                Grid.Column="1"
                HorizontalOptions="End"
                Text="{Binding Quantity}" />

            <labels:BodyLabel
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Text="{Binding Description}" />

        </Grid>
    </Border>
</ContentView>
